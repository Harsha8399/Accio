import React from 'react'

function Menu({items}) {

    const [breakfast, dinner] = items.map(items.category===category)  
  return (
    <div className='App'>Menu
        restaurents.map(itens.id(map(items.id!==id)))
    </div>
  )
}

export default Menu